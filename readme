实现功能:
    将一个整数N拆成  1   2  4   8  ....2的n次方各个元素的加和
    但是每个元素最多可以有两个
    得到所有的方案
    #####################################
    注意：这里的位数  从1开始
    #####################################
重要变量:
    N              输入整数
    wei_num        N写成二进制数字是多少位
    max_num        由wei_num个位组成的最大三进制数字是多少
    fanhuizhi      该三进制数字
    eq_two_num     该三进制数等效的二进制数
编程思路:
    N如果用二进制的话，可以写成多少位，即wei_num
    然后求出由wei_num个数字组成的三进制数，最大是多少，即max_num
    然后遍历1-max_num
         先求出三进制数
         然后转化为等效的二进制数
         最后看其等效的十进制数是多少(fanhuizhi)
         如果该数(eq_two_num)等于输入数字N
         则输出该策略
